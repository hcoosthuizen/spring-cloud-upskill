server:
  port: 8081

spring:
  application:
    name: my-configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hcoosthuizen/spring-cloud-config-upskill.git

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8448/eureka/